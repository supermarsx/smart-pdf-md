name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  USE_UV: '0'

jobs:
  build:
    name: Build ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-x86_64
            py: '3.11'
          - os: ubuntu-22.04-arm64
            target: linux-arm64
            py: '3.11'
          - os: macos-13
            target: macos-x86_64
            py: '3.11'
          - os: macos-14
            target: macos-arm64
            py: '3.11'
          - os: windows-latest
            target: windows-x86_64
            py: '3.11'
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.py }}
          cache: 'pip'
          cache-dependency-path: |
            requirements.txt
            requirements-dev.txt
            requirements.in
            requirements-dev.in
            pyproject.toml
      - name: Setup uv (optional)
        if: env.USE_UV == '1'
        uses: astral-sh/setup-uv@v5

      - name: Install build deps
        run: |
          if [ "${{ env.USE_UV }}" = "1" ]; then
            uv pip install --system pyinstaller pymupdf marker-pdf
          else
            python -m pip install -U pip
            python -m pip install pyinstaller pymupdf marker-pdf
          fi

      - name: Build binary
        run: |
          pyinstaller -F -n smart-pdf-md src/smart_pdf_md/__main__.py

      - name: Rename artifact (non-Windows)
        if: runner.os != 'Windows'
        run: |
          chmod +x dist/smart-pdf-md || true
          mv dist/smart-pdf-md "dist/smart-pdf-md-${{ matrix.target }}"

      - name: Rename artifact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Move-Item dist/smart-pdf-md.exe "dist/smart-pdf-md-${{ matrix.target }}.exe"

      - name: Checksums
        shell: bash
        run: |
          cd dist
          for f in *smart-pdf-md*; do
            shasum -a 256 "$f" > "$f.sha256" || sha256sum "$f" > "$f.sha256" || true
          done

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bin-${{ matrix.target }}
          path: dist/*

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: bin-*
          merge-multiple: true

      - name: Generate release notes
        id: notes
        run: |
          tag="${GITHUB_REF##*/}"
          echo "tag=$tag" >> $GITHUB_OUTPUT
          echo "name=Release $tag" >> $GITHUB_OUTPUT

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.notes.outputs.tag }}
          name: ${{ steps.notes.outputs.name }}
          generate_release_notes: true
          files: |
            dist/*
